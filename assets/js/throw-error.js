/* function funcstp (a,b) {
  if (typeof a !== 'number' || typeof b !== 'number') {
    throw new TypeError('error');
  }
    const sum = Math.pow(a,b);
    return sum;
}

const result = funcstp(5,2);
console.log(result);
 */

////////////////////////////////////////
/* function funcNum(num) {
  if (typeof num !== 'number' || isNaN(num)) {
    throw new TypeError("Owibka");
  }if (num <= 10) {
    return true;
  } else throw new Error('owibka')
}

const res = funcNum(10);
console.log(res);
 */
////////////////////////////////////////
